== Rails Blog with Behavior Driven Development

This is a very simple Rails blog developed with behavior-driven development with RSpec and Capybara.

=== Commands used to create the application
- To create a new Rails app without the default tests, run 'rails new rails-bdd-blog -T'
- After installing the rspec-rails gem, run 'rails g rspec:install'

